package tannus.storage.core;

/**
  * Enum of all Index Types
  */
enum IndexType {
	/* The 'float' type */
	ITFloat;

	/* The 'int' type */
	ITInt;

	/* The 'bool' type */
	ITBool;

	/* The 'string' type */
	ITString;

	/* The 'bytes' type */
	ITBytes;

	/* The 'date' type */
	ITDate;

	/* The 'array' or 'list' type */
	ITArray;

	/* The 'dict' type */
	ITDict;
}
